# Developer's corner 
## Tests

* The versions of python that are tested are listed in `tox.ini`.
* To run the test, run:
```bash
tox -e py
```

## Documentation

* To build the documentation, run:
```bash
make docs
```
